%p#notice= notice
%h1 Workouts
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @workouts.each do |workout|
      %tr
        %td= workout.workout_name
        %td= workout.weight_set.numb_of_sets
        %td= workout.weight_set.weight_rep.numb_of_reps
        %td= workout.weight_set.weight_rep.amount_of_weight

        %td= link_to 'Edit', edit_workout_path(workout)
        %td= link_to 'Delete', workout, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Workout', new_workout_path
