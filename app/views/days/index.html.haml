%p#notice= notice
%h1 Days
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @days.each do |day|
      %tr
        %td= day.day_of_week

        %td= link_to 'Show', [day.week, day]
        %td= link_to 'Edit', edit_week_day_path(day.week, day)
        %td= link_to 'Destroy', [day.week, day], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Day', new_week_day_path
|
= link_to 'Back to Weeks', weeks_path
